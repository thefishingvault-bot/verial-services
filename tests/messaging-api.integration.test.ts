import { describe, it } from "vitest";

// Integration scaffolding placeholder. Real API tests should spin up the app
// with test data. Marked skipped to avoid false failures in CI until fixtures
// are wired.
describe.skip("messaging API integration", () => {
  it("sends, stores, and delivers a message", () => {});
  it("marks read and updates unread counts", () => {});
  it("rejects unauthorized access", () => {});
});
