import { test } from "@playwright/test";

// Placeholder E2E flow; skipped until test fixtures and data factories are ready.
test.describe.skip("Messaging E2E", () => {
  test("customer and provider exchange messages with realtime updates", async () => {
    // TODO: seed booking, sign in as customer, open messages, send, switch to provider, verify realtime updates.
  });
});
